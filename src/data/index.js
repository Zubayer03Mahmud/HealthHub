/**
 * In-Memory Data Store
 *
 * Collects each feature's dataset into a single object. Every feature keeps
 * its records in its own file so team members never edit the same data.
 *
 * The vaccine feature has moved to a real MySQL database (see
 * src/models/vaccine-model.js) and no longer uses this shared store.
 *
 * @module DataStore
 */

module.exports = {
	// TEAM: add your dataset below, one line each, if your feature still
	// uses the shared in-memory store.
	// Example: hospitals: require( './hospitals' )
};